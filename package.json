{
    "name": "@razor/root",
    "private": true,
    "version": "1.0.0",
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "lerna run test",
        "test:scope": "lerna run test --scope={@razor/common,@razor/web}",
        "new-version": "lerna version --conventional-commits --yes",
        "diff": "lerna diff"
    }
}
